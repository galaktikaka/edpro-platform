# EdPro — Платформа онлайн-курсов

Онлайн-платформа для продажи и прохождения обучающих курсов, созданная на Django.

## 🚀 Возможности

- **Каталог курсов** с фильтрацией по категории, уровню и цене
- **Корзина и оформление заказов** — полноценная система покупки курсов
- **Роли пользователей** — студенты и преподаватели
- **Онлайн-помощник** — FAQ по категориям с формой обратной связи
- **Простая аналитика** — топ-5 курсов и выручка за месяц
- **Тест подбора курса** — рекомендации на основе предпочтений
- **Проекты студентов** — витрина работ выпускников
- **Система отзывов** и рейтингов

## 📋 Требования

- Python 3.8+
- Django 5.2.8

## 🛠️ Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <ваш-репозиторий>
cd lab11
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**macOS/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Применение миграций

```bash
cd studyhub
python manage.py makemigrations courses
python manage.py migrate
```

### 5. Создание суперпользователя (для доступа к админке)

```bash
python manage.py createsuperuser
```

### 6. Запуск сервера разработки

```bash
python manage.py runserver
```

Откройте браузер и перейдите на `http://127.0.0.1:8000/`

## 📁 Структура проекта

```
lab11/
├── studyhub/              # Основной проект Django
│   ├── courses/          # Приложение курсов
│   │   ├── models.py     # Модели данных
│   │   ├── views.py      # Представления
│   │   ├── urls.py       # URL-маршруты
│   │   ├── admin.py      # Админ-панель
│   │   └── templates/    # Шаблоны HTML
│   ├── studyhub/         # Настройки проекта
│   │   ├── settings.py   # Конфигурация
│   │   └── urls.py       # Главные URL
│   └── manage.py         # Утилита управления
├── requirements.txt      # Зависимости Python
└── README.md            # Этот файл
```

## 🎯 Основные страницы

- `/` — Главная страница
- `/courses/` — Каталог курсов
- `/tutors/` — Список преподавателей
- `/projects/` — Проекты студентов
- `/assistant/faq/` — Онлайн-помощник (FAQ)
- `/assistant/test/` — Тест подбора курса
- `/cart/` — Корзина
- `/checkout/` — Оформление заказа
- `/orders/` — История заказов
- `/admin-stats/` — Аналитика (только для staff)

## 👥 Роли пользователей

- **Студент** — может покупать курсы, оставлять отзывы, проходить обучение
- **Преподаватель** — может создавать и редактировать курсы

Роль выбирается в профиле пользователя.

## 🗄️ Модели данных

- `Course` — курсы (цена, уровень, категория, популярность)
- `Order` / `OrderItem` — заказы и позиции заказов
- `Review` — отзывы о курсах
- `Enrollment` — запись на курс
- `AssistantCategory` / `AssistantQuestion` — FAQ ассистента
- `SupportRequest` — обращения в поддержку
- `UserProfile` — расширенный профиль пользователя (роль)

## 🔧 Настройка для VS Code

1. Откройте папку проекта в VS Code
2. Выберите интерпретатор Python: `Ctrl+Shift+P` → "Python: Select Interpreter" → выберите `venv/bin/python`
3. Рекомендуемые расширения:
   - Python
   - Django
   - Python Docstring Generator

## 📝 Создание миграций

После изменения моделей:

```bash
python manage.py makemigrations courses
python manage.py migrate
```

## 🎨 Админ-панель

Доступ: `http://127.0.0.1:8000/admin/`

В админке можно управлять:
- Курсами (цена, уровень, популярность)
- Заказами и обращениями
- Категориями и вопросами FAQ
- Профилями пользователей

## 📄 Лицензия

Учебный проект.

## 👨‍💻 Автор

Создано для изучения Django и веб-разработки.

