# 🚀 Инструкция по настройке проекта в VS Code и GitHub

## Шаг 1: Настройка Git (если еще не настроен)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

## Шаг 2: Создание репозитория на GitHub

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New repository"** (или **"+"** → **"New repository"**)
3. Заполните:
   - **Repository name**: `edpro-platform` (или любое другое имя)
   - **Description**: "Платформа онлайн-курсов EdPro на Django"
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add README", "Add .gitignore" (у нас уже есть)
4. Нажмите **"Create repository"**

## Шаг 3: Подключение локального репозитория к GitHub

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
cd /Users/galaktikaka/Downloads/lab11
git remote add origin https://github.com/ВАШ_USERNAME/edpro-platform.git
git branch -M main
git push -u origin main
```

**Важно:** Замените `ВАШ_USERNAME` на ваш логин GitHub и `edpro-platform` на имя вашего репозитория.

## Шаг 4: Открытие проекта в VS Code

1. Откройте VS Code
2. **File** → **Open Folder** (или `Cmd+O` на Mac)
3. Выберите папку `/Users/galaktikaka/Downloads/lab11`

## Шаг 5: Настройка Python интерпретатора в VS Code

1. Нажмите `Cmd+Shift+P` (Mac) или `Ctrl+Shift+P` (Windows/Linux)
2. Введите: `Python: Select Interpreter`
3. Выберите интерпретатор из виртуального окружения:
   - `./venv/bin/python` (Mac/Linux)
   - `.\venv\Scripts\python.exe` (Windows)

## Шаг 6: Активация виртуального окружения

**В терминале VS Code** (`Ctrl+`` или `View` → `Terminal`):

**Mac/Linux:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

## Шаг 7: Установка зависимостей (если нужно)

```bash
pip install -r requirements.txt
```

## Шаг 8: Применение миграций

```bash
cd studyhub
python manage.py makemigrations courses
python manage.py migrate
```

## Шаг 9: Создание суперпользователя

```bash
python manage.py createsuperuser
```

Введите:
- Username: `admin` (или любое другое)
- Email: `admin@example.com`
- Password: (придумайте пароль)

## Шаг 10: Запуск сервера

```bash
python manage.py runserver
```

Откройте браузер: `http://127.0.0.1:8000/`

## 📦 Рекомендуемые расширения VS Code

Установите через `Cmd+Shift+X` (Extensions):

1. **Python** (Microsoft) — основное расширение для Python
2. **Django** (Baptiste Darthenay) — поддержка Django
3. **Python Docstring Generator** — генерация документации
4. **GitLens** — улучшенная работа с Git
5. **Error Lens** — подсветка ошибок в коде

## 🔄 Работа с Git в VS Code

### Просмотр изменений:
- Откройте вкладку **Source Control** (`Ctrl+Shift+G`)
- Видите все измененные файлы

### Коммит и пуш:
1. Напишите сообщение коммита
2. Нажмите `Cmd+Enter` (Mac) или `Ctrl+Enter` (Windows)
3. Нажмите `...` → **Push** для отправки на GitHub

### Создание новой ветки:
1. Внизу слева нажмите на название ветки (`main`)
2. Выберите **"Create new branch"**
3. Введите имя ветки

## ⚠️ Важные замечания

- **Не коммитьте** `db.sqlite3` — база данных должна быть локальной
- **Не коммитьте** файлы из `venv/` — виртуальное окружение
- **Не коммитьте** секретные ключи — используйте `.env` файл для `SECRET_KEY`

## 🐛 Если что-то не работает

1. **Проверьте интерпретатор Python** в VS Code
2. **Убедитесь, что виртуальное окружение активировано**
3. **Проверьте, что все зависимости установлены**: `pip list`
4. **Проверьте миграции**: `python manage.py showmigrations`

## 📚 Полезные команды Git

```bash
# Посмотреть статус
git status

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить изменения с GitHub
git pull

# Посмотреть историю коммитов
git log
```

## 🎉 Готово!

Теперь ваш проект настроен и готов к работе в VS Code и GitHub!

